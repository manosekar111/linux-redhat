 df -hT  to check the availablity

Then check resize2fs package is available or not 

sudo zypper install xfsprogs#( check whether the package is available are not)
sudo xfs_growfs -d /sybase #(/sybase is the volume name that should be resized)


